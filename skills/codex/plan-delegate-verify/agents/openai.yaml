interface:
  display_name: "Plan, Delegate, Verify"
  short_description: "Persistent cost-aware multi-agent orchestration"
  default_prompt: "Use $plan-delegate-verify to orchestrate this objective persistently: plan, route, delegate, verify, integrate, measure, and re-plan until completion or a concrete blocker."
